{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "pnpm exec prisma generate && nodemon --inspect server.js",
    "start": "node server.js",
    "postinstall": "pnpm exec prisma generate",
    "seed": "pnpm exec prisma generate && node migration/seed.js",
    "db:setup": "pnpm exec prisma migrate deploy && pnpm exec prisma generate && node migration/seed.js",
    "db:export": "node migration/export-db.js",
    "build:frontend": "pnpm install --dir ../frontend && pnpm --dir ../frontend run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/adapter-mariadb": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "@prisma/client-runtime-utils": "^7.9.1",
    "bcrypt": "^6.0.0",
    "cloudinary": "^2.10.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mariadb": "^3.5.3",
    "mongoose": "^9.7.0",
    "multer": "^2.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "prisma": "^7.8.0"
  }
}
